[rpc]
host = "127.0.0.1"
port = 8080

[database]
# Persistent volume mounted at /data in the Render container
path = "/data/asm-db"

[bitcoin]
rpc_url = "http://127.0.0.1:18443"
rpc_user = "user"
rpc_password = "password"
rawblock_connection_string = "tcp://127.0.0.1:28332"
