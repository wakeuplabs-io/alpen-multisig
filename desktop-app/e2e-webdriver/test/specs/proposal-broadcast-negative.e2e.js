/**
 * P-032 — broadcast negative paths (Wave 2 partial).
 * Run after happy-path quorum spec; requires full local stack.
 *
 * npm run test:e2e:proposal-broadcast-negative
 */
describe('proposal broadcast negatives (P-032)', () => {
	it.skip('shows error when broadcast RPC is unreachable', async () => {
		// TODO: wire when US-H5 / negative matrix scope is approved
	})
})
