# Broadcast uses the orchestrator backend only (operator key and Bitcoin RPC live in orchestrator-be/.env).
# Copy to .env when customizing the orchestrator URL.

# Optional — default matches local orchestrator
# VITE_ORCHESTRATOR_BASE_URL=http://127.0.0.1:3000/api/v1
