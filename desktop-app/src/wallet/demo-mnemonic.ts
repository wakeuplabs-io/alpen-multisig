/** Pre-production demo seed — must match E2E + Rust fixtures. Remove before prod release. */
export const DEMO_MNEMONIC = 'multiply toss magic exclude crawl obey garden black apart room village neglect'
