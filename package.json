{
  "name": "alpen-multisig",
  "private": true,
  "workspaces": [
    "desktop-app"
  ],
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "dev:desktop-app": "npm run dev --workspace desktop-app",
    "build:desktop-app": "npm run build --workspace desktop-app",
    "tauri:desktop-app": "npm run tauri --workspace desktop-app",
    "validate": "cargo fmt --check && npm run format:check --workspace desktop-app && cargo build --workspace --all-targets && cargo clippy --workspace --all-targets -- -D warnings"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
